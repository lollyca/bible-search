'use client'

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#a49a9a"
    },
    secondary: {
      main: "#fdf2f2"
    },
  },
});

export default theme;